---
name: dji-media-organizer
description: |
  把 DJI Osmo / Action 等设备导出的原始素材，按实际拍摄日期整理为 `YYYYMMDD` 子目录。
  用户指定目录时就扫该目录（递归，含其中的 `DJI_001` 等编号子目录），并把日期目录建在
  该目录下；没指定就扫当前项目目录。执行前必须先问是否清理 `.LRF` 预览文件。
  触发：用户说「整理」「按日期分类」「扫描 DJI_001」「把素材归档」「按年月日移动」，
  或提到 `DJI_001`~`DJI_003` 目录，或描述把 DJI 编号子目录里的 MP4/JPG 按拍摄日期归档。
  只用于**归档已有素材**，不用于「找素材」——不要为此搜索目标目录以外的地方。
  不适用：视频转码剪辑、素材去重、上传或同步到云盘、非 DJI 相机且文件名不含
  `YYYYMMDD_HHMMSS` 的素材。
---

# DJI Media Organizer

把 `DJI_<YYYYMMDD>_<HHMMSS>_<序号>_<后缀>.<扩展名>` 形式的素材，按文件名 + 元数据双重校验后的
拍摄日期，归档到 `YYYYMMDD/` 子目录。素材可以在编号子目录里（`DJI_001/`），也可以直接散在
目标目录下。

1. **先报计划，再动文件。**
2. **删任何东西之前必须显式确认。** `.LRF` 每次都要问。
3. **宁可停下问，不要猜。** 日期、时区、同名冲突、跨卷，任何一项对不上就停下。
4. **绝不重复归档。** 已有的 `YYYYMMDD/` 目录永远是输出，不是输入。

## Procedure

### 1. 确定扫描目标与归档根

按顺序判断：

1. **用户指定了目录** → 扫描目标就是该目录。用户给的可能是 `DJI_001/` 本身，也可能是一个
   装着一堆素材的普通目录；两种情况都按同一个规则处理。
2. **用户没指定** → 扫描目标是**当前执行的项目目录**（workspace 根）。

**归档根 = 扫描目标**，即 `YYYYMMDD/` 建在扫描目标下。绝大多数情况下扫描目标是 SD 卡上的
相机目录，日期目录因此与 `DJI_001/` 平级；若用户直接把 `DJI_001/` 指定为扫描目标，
日期目录就建在 `DJI_001/` 内——以用户指定的目录为准，不要擅自上移一层或下移一层。

扫描目标不存在、不可读、或是单个文件 → 停下报告，不要自己换一个目录继续。

**只在扫描目标这一个目录树里找素材。** 目标里没有可归档素材（没有 DJI 命名的媒体文件）时，
就地停下、报告你看到了什么，然后问用户要扫哪里——**不要**顺手去翻别的目录、别的盘、
别的挂载卷（`/Volumes/`、`~/Movies`、`~/Desktop` 之类）来"帮他找素材"。
发现别处可能有素材时，把路径作为**建议**报给用户，等用户确认或指定后再扫。

### 2. 递归扫描素材

- **递归**扫描目标目录整棵子树，深度不限：素材可能在 `DJI_001/`、`DJI_002/`，也可能直接
  放在目标目录下。不用要求目标里有 `DJI_NNN` 子目录。
- **跳过归档产物**：任何 `YYYYMMDD` 形式的目录（以及其中的 `LRF/`）是之前的输出，整棵跳过。
  同时跳过 `._*` 影子文件和 `.DS_Store`。这是防止二次归档的关键。
- 收集主体文件 `.MP4` / `.JPG` / `.DNG` / `.MOV`（大小写不敏感），以及同名 sidecar
  `.SRT` / `.THM`。

```sh
find "$目标" \
  \( -name '._*' -o -name '.DS_Store' \
     -o -path '*/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]' \
     -o -path '*/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]/*' \) -prune -o \
  -type f \( -iname '*.MP4' -o -iname '*.JPG' -o -iname '*.DNG' -o -iname '*.MOV' \
             -o -iname '*.SRT' -o -iname '*.THM' \) -print
```

`-path` 的两条数字模式分别匹配日期目录本身和它里面的所有内容，整棵剪掉；
用数字模式而不是 `-regex`，因为 `-regex` 在 BSD 与 GNU 上语法不一致。
非 POSIX 环境（如 Windows）用等价方式递归遍历，语义保持一致。

**退出条件**：目标全树已扫描；`.LRF` 数量与总大小已统计；没有任何归档产物被当成素材。

### 3. 分类：文件名带日期 / 不带日期

**情况 A —— 文件名含可解析日期**（`DJI_<YYYYMMDD>_<HHMMSS>_…`，且 `YYYYMMDD` 是合法日期）：

- 后缀可能是 `_D` / `_001` / `_002` 等，不要写死成 `_D`。
- 统计**日期分布**（每个 `YYYYMMDD` 各多少文件），并按第 5 步用元数据交叉校验。
- 文件名可能含空格或中文，遍历变量一律加双引号。

  ```sh
  case "$(basename "$f")" in
    DJI_[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]_[0-9][0-9][0-9][0-9][0-9][0-9]_*.*) 情况A ;;
    *) 情况B ;;
  esac
  ```

  模式只保证 8 位数字，**不校验日期是否合法**（`20261332` 也能匹配）。所以要额外检查月和日：
  `2026` `13` `32` 这种伪日期归入情况 B，不要当日期用。

**情况 B —— 文件名不含 DJI 格式的日期**，分三类处理：

- **同名 sidecar**：`.SRT` / `.THM` 的基名与某个视频/照片相同（如 `X.MP4` 与 `X.SRT`）
  → **继承主体的日期**，跟主体进同一个 `YYYYMMDD/`。这类不算异常。
- **文件名不是 DJI 格式，但能从中读出 `YYYYMMDD`**（如 `VID_20260920.mp4`）：
  先按该日期归档，同时用元数据交叉校验；对不上就停下问用户。
- **文件名里任何位置都读不出日期** → 归入「无日期」清单，交给第 5 步用元数据定日期；
  元数据也拿不到就报给用户决定（见 Failure handling），**不要丢进日期目录，也不要直接丢进垃圾堆**。

**退出条件**：每个文件都被归入「情况 A」「名字里有日期但非 DJI 格式」「继承主体的 sidecar」
「无日期（待第 5 步）」四者之一。

### 4. 询问 `.LRF` 处置（不能默认跳过）

问用户，标题「DJI LRF 文件清理」，选项里带**具体数字**：

| 选项 | 行为 |
|---|---|
| 删除并继续整理（推荐） | 每个 `.LRF` 及其 `._*.LRF` 影子移入废纸篓，然后继续 |
| 保留并整理 | `.LRF` 归档到 `<归档根>/YYYYMMDD/LRF/` |
| 取消整个流程 | 直接结束，一个文件都不动 |

- **逐个文件**删除，不要用通配符一次删一批——容易被安全策略判定为批量删除而拦截。
  优先用可恢复的删除方式，探测本机有什么可用：

  ```sh
  if command -v trash >/dev/null 2>&1; then trash -s "$f"; else rm -f -- "$f"; fi
  ```

  - `trash` 必须加 `-s`（stop-on-error）：默认行为是**部分文件删不掉也照样返回成功**。
  - **macOS 自带的 `trash` 不认 `--` 选项终止符**（`trash -s -- "$f"` 会去删一个字面叫 `--`
    的文件并报错）。传给它的路径用绝对路径，或以 `./` 开头的相对路径，避免文件名以 `-` 开头被当选项。
    `rm` / `mv` / `dot_clean` 都正常支持 `--`，别因为 `trash` 而把整段都去掉。
  - 只能退化到 `rm` 时，选项文案里必须写明「不可恢复」。
- 删除后用 `du -sk` 复核磁盘释放量，写进最终报告。

**退出条件**：用户已明确选择。

### 5. 用元数据定日期并交叉校验

1. 逐文件读元数据：
   - 视频：`ffprobe -v quiet -print_format json -show_format -- "$f"` →
     `format.tags.creation_time`（ISO 8601，通常是 UTC，带 `Z` 或 `+00:00`）。
   - JPG：macOS 用 `sips -g creation -- "$f"`，取不到再用
     `mdls -name kMDItemContentCreationDate -- "$f"`；其它平台用
     `exiftool -s3 -DateTimeOriginal -- "$f"`。
2. **探测偏移，不要写死 `+8h`**：DJI 部分机型把本地时间当 UTC 存储。
   只用**情况 A**（文件名带日期）的文件试候选偏移（如 `0 / +8 / +9 / -4 / -5 / -7`），
   统计「`creation_time + 偏移` 的 `YYYYMMDD` 与文件名日期一致」的比例，取一致率最高的。
   情况 A 的样本越多越可靠；不足 3 个样本时直接让用户确认偏移。
3. 算日期只用**整小时** `-v+8H`，不要用 `-v+480M`（跨 DST 会算错日）：
   - BSD/macOS：`TZ=UTC date -j -f '%Y-%m-%dT%H:%M:%S' '<creation_time>' -v+8H '+%Y%m%d'`
   - GNU/Linux：`TZ=UTC date -d '<creation_time> +8 hours' '+%Y%m%d'`
4. 一致率必须是 **100%**，并把探测到的偏移和一致率报给用户。
   提取失败或一致率不到 100% → **停下**报不一致清单。同一批次内两者本应一致，
   不一致说明换了相机、相机时区设置不同或跨了 DST。
5. **给「无日期」文件定日期**（第 3 步的情况 B 里除 sidecar 之外的）：
   - 用探测到的同一个偏移，把元数据时间换算成 `YYYYMMDD`，归档到对应日期目录。
   - 整批都是无日期文件、导致偏移无从探测时：不要猜，让用户指定偏移或直接按当地时区换算。
   - 元数据也拿不到时间 → 不要自行归入某一天，按 Failure handling 交给用户决定。
   - 每个这样定出来的日期都要在报告里标注为「由元数据推断」，不要和文件名日期混在一起报。

**退出条件**：情况 A 全部通过校验；无日期文件要么拿到了（标注来源的）日期，要么已交给用户拍板。

### 6. 生成归档计划并让用户确认

先输出一张表：**每个 `YYYYMMDD` → 文件数 + 总大小**，以及合计。同时算清四件事：

- **同名冲突**：归档根的目标日期目录已存在同名文件 → 列出清单，问用户跳过 / 加后缀 / 覆盖。
  绝不静默覆盖。
- **磁盘空间**：归档卷可用空间必须大于待移动总字节数（同卷内移动不额外占空间）。不足就停下报告。
- **同卷判断**：扫描目标与归档根是同一卷则移动是瞬时的；跨卷则第 7 步改用 rsync。
  两者通常是同一个目录，天然同卷。
- **来源分布**：素材来自哪些子目录（如 `DJI_001/` 12 个、根目录散落 3 个），一并列出，
  便于用户确认扫对了地方。

**退出条件**：用户确认了这张计划表。

### 7. 建日期目录并移动

- 在**归档根**（即扫描目标）下建目录，不要在 `DJI_001/` 里面建。
- 逐个文件移动，`--` 终止选项解析，`-n` 避免覆盖：

  ```bash
  src="$f"; dst="$归档根/$d/$(basename "$f")"
  if [ ! -e "$dst" ]; then mv -n -- "$src" "$dst"; fi
  ```

  路径含空格或中文（如 `/Volumes/dji 共享给我/`）时变量必须加双引号。
  不要用 `mv *.MP4 20260920/` 这种多文件通配写法。
- 跨卷时用 `rsync -a --remove-source-files --ignore-existing`（可断点续传）；
  完成后核对源目录已空、字节数一致。
- 大批量（约 >20 个文件或 >50 GB）时交给后台执行，等它跑完再继续；
  **不要中途再起一条 mv**，否则会重复搬运或产生半截文件。

**退出条件**：可归档文件已全部离开原位；编号子目录只剩 `.DS_Store` 或空。

### 8. 清理 macOS `._` AppleDouble 影子文件

- 对每个新建的日期目录执行 `dot_clean -fm -- <日期目录>`：
  `-m` 才会真正**删除** `._*`（不加则只合并不删），`-f` 不递归（不加会递归整棵树）。
- 非 macOS 平台跳过本步。
- `dot_clean` 失败时改成逐个 `rm -f -- "._xxx"`；残留逐个列给用户，不要递归扫整个卷。

**退出条件**：日期目录内已无 `._*`，或残留清单已报告。

### 9. 校验并汇报

- 数量与**字节数**都要校验：`du -sk` 对比移动前扫描到的合计与移动后各日期目录的合计。
- 对不上 → 不要报成功，把差值和可能原因报给用户。

## Output contract

- **归档根**（即扫描目标）下出现若干 `YYYYMMDD/` 子目录，内含该日期的 MP4 / JPG，**原文件名不改**。
- sidecar 与主体文件在同一日期目录。
- 素材原位置清空（编号子目录最多剩 `.DS_Store`）；原有的归档目录未被改动。
- `.LRF` 按用户选择处理；AppleDouble `._*` 已清理或已报告残留。

扫描目标里没有可归档素材时，**不产出上面任何东西**：如实报告目标位置与看到的内容，
然后停下等用户指定目录。空手而归是合法结果，不要为了"有产出"而去别处找活干。

回复用户的报告必须包含：

| 项 | 内容 |
|---|---|
| 每个日期目录 | 文件数 + 占用大小 |
| 合计 | 文件总数 + 总大小 |
| 校验 | 移动前后文件数与字节数是否一致 |
| LRF 处置 | 删除 N 个 / 释放 X GB，或归档到何处 |
| 时间校验 | 探测到的元数据偏移 + 一致率 |
| 无日期文件 | 哪些、各用了哪种处理（元数据推断 / 未标注日期 / 用户指定日期） |
| 异常 | 时间不一致、未清的影子文件、按用户选择跳过的文件 |

## Failure handling

- **目标里有多个 `DJI_NNN` 子目录** → 正常情况，全部素材汇总归档到归档根，不需要逐个问。
- **重跑时把已有 `YYYYMMDD/` 当素材又归档一次** → 错误。第 2 步必须排除归档产物。
- **没问 LRF 就直接开整理** → 错误，必须先问。
- **扫描目标不存在/不可读/是文件** → 停下报告，不要自己换目录。
- **扫描目标里没有可归档素材** → 停下报告，不要改扫别处。只把别处可能的素材路径作为建议列出。
- **目标目录已有同名文件** → 不覆盖，列出冲突清单让用户选。
- **文件名不含日期，但元数据有拍摄时间** → 用元数据定日期并归档，报告里标注「由元数据推断」。
- **文件名和元数据都没有日期** → 三种处理，让用户选：跳过不动 / 原样移到
  `<归档根>/未标注日期/` / 用文件系统时间（`mtime` 或 `birthtime`）当日期。
  第三种只是兜底：拷贝、下载、跨卷搬运都会改掉它，日期可能整体偏移，选它时必须写明这一点。
- **元数据与文件名日期不一致** → 停下报告，不要硬猜日期，也不要混着归档。
- **候选偏移不唯一或一致率不到 100%** → 停下报告。
- **`ffprobe` 缺失或读不到 `creation_time`** → 先试 `mdls`；仍拿不到就按「文件名和元数据都没有日期」处理。
- **目标卷只读或空间不足** → 中止并报告；已移动的部分要如实汇报。
- **跨卷移动中断** → 用 `rsync -a --remove-source-files --ignore-existing` 续跑。
- **`dot_clean` 失败或影子文件残留** → 逐个删；清不掉就列出来。
- **移动命令被安全策略拦截**（如 "dangerous removal target"）→ 改回逐文件 `mv -n --`。
- **命令被转成后台/异步执行** → 等它结束再继续，不要中途再起一条移动命令。
- **删除 LRF 后磁盘没释放** → 报路径和剩余空间，不要自动再清一遍。

## Examples

**Input**：「整理 /Volumes/dji 共享给我/DJI Device/OsmoNano-CBF0/」

**过程**：扫描目标 = 该目录（递归）→ 归档根 = 同一目录 →
在 `DJI_001/` 下扫到 81 个 MP4、47 个 LRF → 探测偏移 `+8h`、一致率 100% →
报计划表 → 用户确认 → 建 `20260920/`、`20260921/` 并逐个移动 → 清理影子文件 → 校验。

**Output**：

```
OsmoNano-CBF0/
  DJI_001/              ← 清空，只剩 .DS_Store
  20260920/             ← 27 个文件 (28 GB)
  20260921/             ← 54 个文件 (64 GB)
```

| 目录 | 文件数 | 大小 |
|---|---|---|
| 20260920 | 27 | 28 GB |
| 20260921 | 54 | 64 GB |
| 合计 | 81 | 92 GB |

校验：移动前后均为 81 个文件、92 GB。
LRF：删除 47 个，释放 812 MB。
时间校验：偏移 `+8h`，一致率 100%。异常：无。

**Input**：「整理一下」/「按日期分类」（未指定目录）

**Output**：扫描目标 = 当前项目目录，归档根 = 当前项目目录。其余同上。

**Input**：目录里混着非 DJI 命名的文件（`VID_20260920.mp4`、`video_01.mp4`、伪日期 `DJI_20261332_...MP4`）

**过程**：`VID_20260920.mp4` 能读出日期但格式不是 DJI → 按 `20260920` 归档并用元数据校验；
`video_01.mp4` 和 `DJI_20261332_...MP4` 读不出日期 → 进「无日期」清单 →
它们元数据里有 `creation_time`，用探测到的 `+8h` 换算成实际日期归档，
报告里标注「由元数据推断」，与文件名日期分开列。

**若元数据也没有时间**：把清单交给用户，让他在「跳过 / 移到 `未标注日期/` / 用文件系统时间」
三者里选，不要自己挑一个。

## 依赖

缺任何一项都有降级路径，不要自动安装。

| 工具 | 用途 | 缺失时 |
|---|---|---|
| `ffprobe` | 读视频拍摄时间 | 改用 `mediainfo`；或退化为仅按文件名日期，需用户确认 |
| `mv` / `du` / `find` | 移动与校验 | 无替代 |
| `rsync` | 跨卷移动、可续传 | 逐个 `cp -p` + 校验后删源 |
| `sips` / `mdls` | macOS 读 JPG 时间 | 非 macOS 用 `exiftool -DateTimeOriginal` |
| `dot_clean` | 清理 `._` 影子文件（仅 macOS） | 逐个 `rm -f` |
| `trash` | 可恢复删除 LRF | 只能 `rm` 时须提示不可恢复 |
